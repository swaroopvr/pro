<!--<h1>ALL LK_TBL_TYPE_TBLS </h1>

<% @lk_up_type.each do |f| %>
	<div class="theShower text-center">
		<div><%=f.LK_TBL_TYPE%></div>
	</div>
<% end %>

<%= link_to 'New', new_lk_tbl_type_tbl_path %>-->


<div class="table-view">
	<table>
		<caption>LIST OF BIT_SEL COMMANDS</caption>
		<thead>
			<tr>
				<th> ID </th>
				<th> LKUP_TBL_TYPE </th>

			</tr>
		</thead>
		<tbody>
		<% @lk_tbl_type.each do |f| %>
		<tr data-href="<%= edit_lk_tbl_type_tbl_path(f) %>"  class="theShower text-center">
	
		
		<td><%= f.ID %></td>
				<td><%= f.LKUP_TBL_TYPE %></td>
				<% if current_user.admin? %>	<!-- add this too -->
					<td class="deleter"><%= link_to "x", f , method: :delete, data: { confirm: "You sure?" } %></td>	<!-- add this too -->
				<% end %>
			
		</tr>	
		<% end %>
		</tbody>
	</table>

	</div>
	<%= link_to 'Add', new_lk_tbl_type_tbl_path, class: "add-new-table" %