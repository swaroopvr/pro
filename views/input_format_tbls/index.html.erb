


<div class="table-view">
	<table>
		<caption>LIST OF INPUT_FORMATS</caption>
		<thead>
			<tr>
				<th> ID </th>
				<th> INPUT_FORMAT</th>

			</tr>
		</thead>
		<tbody>
		<% @input_format.each do |f| %>
		<tr data-href="<%= edit_input_format_tbl_path(f) %>"  class="theShower text-center">
	
		
		<td><%= f.ID  %></td>
				<td><%= f.INPUT_FORMAT %></td>
				<% if current_user.admin? %>	<!-- add this too -->
					<td class="deleter"><%= link_to "x", f , method: :delete, data: { confirm: "You sure?" } %></td>	<!-- add this too -->
				<% end %>
			
		</tr>	
		<% end %>
		</tbody>
	</table>

	</div>
	<%= link_to 'Add', new_input_format_tbl_path, class: "add-new-table" %>