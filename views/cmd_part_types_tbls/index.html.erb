


<div class="table-view">
	<table>
		<caption>LIST OF CMD_PART_TYPES</caption>
		<thead>
			<tr>
				<th> CMD_PART_TYPE_ID</th>
				<th> CMD_PART_TYPE </th>

			</tr>
		</thead>
<tbody>

<% @cmd_part_types.each do |f| %>
	<tr class="theShower text-center">
		
			<td><%=f.CMD_PART_TYPE_ID%></td>
			<td><%=f.CMD_PART_TYPE%></td>
			<% if current_user.admin? %>	<!-- add this too -->
					<td class="deleter"><%= link_to "x", f , method: :delete, data: { confirm: "You sure?" } %></td>	<!-- add this too -->
				<% end %>
			</tr>
		<% end %>
	</tbody>
</table>
	</div>
	<!--<%= link_to 'New', new_cmd_part_types_tbl_path %>-->
	<%= link_to 'Add', new_cmd_part_types_tbl_path, class: "add-new-table" %>