

<div class="table-view">
	<table>
		<caption>LIST OF EXPR_TYPE</caption>
		<thead>
			<tr>
				<th>  ID</th>
				<th> EXPR_TYPE </th>

			</tr>
		</thead>
<tbody>

<% @expr_type.each do |f| %>
<tr data-href="<%= edit_expr_type_tbl_path(f) %>"  class="theShower text-center">
	
		
						<td><%= f.ID %></td>
				<td><%= f.EXPR_TYPE  %></td>
				<% if current_user.admin? %>	<!-- add this too -->
					<td class="deleter"><%= link_to "x", f , method: :delete, data: { confirm: "You sure?" } %></td>
					<%end%>
			</tr>
  			<% end %>
				</tbody>
	</table>
	</div>


<%= link_to 'Add', new_expr_type_tbl_path, class: "add-new-table" %>