


<div class="table-view">
	<table>
		<caption>LIST OF VALUE_TYPE COMMANDS</caption>
		<thead>
			<tr>
				<th> ID </th>
				<th> VALUE_TYPE </th>

			</tr>
		</thead>
		<tbody>
		<% @value_type.each do |f| %>
		<tr data-href="<%= edit_value_type_tbl_path(f) %>"  class="theShower text-center">
	
		
		<td><%= f.ID  %></td>
				<td><%= f.VALUE_TYPE%></td>
				<% if current_user.admin? %>	<!-- add this too -->
					<td class="deleter"><%= link_to "x", f , method: :delete, data: { confirm: "You sure?" } %></td>	<!-- add this too -->
				<% end %>
			
		</tr>	
		<% end %>
		</tbody>
	</table>

	</div>
	<%= link_to 'Add', new_value_type_tbl_path, class: "add-new-table" %>